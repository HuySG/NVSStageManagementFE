import React from "react";
type RequestCategoryAssetModalProps = {
  taskId: string;
  onClose: () => void;
};

const RequestCategoryAssetModal = ({
  taskId,
  onClose,
}: RequestCategoryAssetModalProps) => {
  return <div>RequestCategoryAssetModal</div>;
};

export default RequestCategoryAssetModal;
